import 'package:flutter/material.dart';
import 'dart:ui';

// Color ColorPrimary = new Color.fromRGBO(250, 252, 255, 0);
// Color ColorPrimary2 = new Color(0xFF4286f4); 0xFF -> the opacity (FF for opaque) 4286f4 -> the hex-color

const colorPrimary = Color(0XFFf2f2f2); // the light grey of background
const colorAccentGreen = Color(0XFF009146); // the green
const colorAccentGrey = Color(0XFFe6e7e9); // the dark grey
const colorAccentBlue = Color(0XFF212a39); // the dark blue of btn
const colorAccentGreyBtn = Color(0XFFe3e4eb);
const colorAccentLightGreen = Color(0XFFb3ffd1);
